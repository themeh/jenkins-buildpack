#!/usr/bin/env bash
# bin/release <build-dir>
cat << EOF
addons: []
config_vars: []
default_process_types:
  web: java -jar jenkins.war --httpPort=\$PORT -XX:PermSize=512M -XX:MaxPermSize=2048M -Xmn128M -Xms1024M -Xmx2048M
EOF
